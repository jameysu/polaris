import styled from 'styled-components';
import {memo} from "react";

const DashboardStyled = styled.div`
  width: 100%;
  height: 100vh;
  display: flex;
  flex-direction: column;
`;


export default memo(DashboardStyled);